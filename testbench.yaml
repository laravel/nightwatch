providers:
  - Laravel\Nightwatch\NightwatchServiceProvider

workbench:
  start: '/'
  install: true
  discovers:
    web: true
    commands: true
    views: true
  build:
    - create-sqlite-db
    - migrate-refresh
